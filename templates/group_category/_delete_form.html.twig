<form method="post" action="{{ path('group_category_delete', {'id': group_category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ group_category.id) }}">
    <button class="btn">Delete</button>
</form>
