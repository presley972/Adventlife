{% extends 'base.html.twig' %}

{% block title %}GroupCategory{% endblock %}

{% block body %}
    <h1>GroupCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ group_category.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ group_category.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ group_category.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('group_category_index') }}">back to list</a>

    <a href="{{ path('group_category_edit', {'id': group_category.id}) }}">edit</a>

    {{ include('group_category/_delete_form.html.twig') }}
{% endblock %}
